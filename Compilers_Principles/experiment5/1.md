### `FunctionAndClassWindow` 类

```python
class FunctionAndClassWindow(QMainWindow):
    """
    显示提供的代码中的函数和类的列表。

    此窗口提供从Python代码中提取的函数和类的列表。在列表中点击函数或类名将在主代码编辑器中导航到其位置。

    属性:
        text_edit (QTextEdit): 主代码编辑器的引用。
    """
    def __init__(self, functions_and_classes, text_edit):
        """
        初始化 FunctionAndClassWindow 类的实例。
        参数:
            functions_and_classes (list): 从Python代码中提取的函数和类的列表。
            text_edit (QTextEdit): 主代码编辑器的引用。

        返回:
            无
        """       
    def initUI(self, functions_and_classes):
        """
        初始化用户界面。
        创建一个列表小部件来显示从Python代码中提取的函数和类的名称，并设置点击事件的处理程序。
        参数:
            functions_and_classes (list): 从Python代码中提取的函数和类的列表。
        返回:
            无
        """
    def onItemClicked(self, item):
        """
        处理列表中的项被点击的事件。
        在主代码编辑器中导航到被点击的函数或类的位置。
        参数:
            item (QListWidgetItem): 列表中被点击的项。
        返回:
            无
        """
```

这些 docstrings 为 `FunctionAndClassWindow` 类及其方法提供了中文描述，帮助理解类的功能和每个方法的作用。

### `AnswerWindow` 类

```python
class AnswerWindow(QMainWindow):
    """
    用于显示运行 Python 代码的结果。
    此类用于在一个新窗口中显示 Python 代码的输出和错误（如果有）。
    属性:
        output (str): 代码执行的输出。
        error (str): 代码执行的错误信息。
    """

    def __init__(self, output, error):
        """
        初始化 AnswerWindow 类的实例。
        参数:
            output (str): 代码执行的输出。
            error (str): 代码执行的错误信息。
        返回:
            无
        """

    def initUI(self):
        """
        初始化用户界面。
        在窗口中显示代码的执行结果和错误信息。
        参数:
            self: 调用此函数的 AnswerWindow 对象。
        返回:
            无。但它会在窗口中显示代码的执行结果。
        """
```

这些 docstrings 为 `AnswerWindow` 类及其方法提供了中文描述，帮助理解类的功能和每个方法的作用。

### `MainWindow` 类

```python
class MainWindow(QMainWindow):
    """
    用于显示 GUI 主界面的类。
    此类用于显示一个包含文本编辑器、菜单栏和其他功能的主窗口，用户可以在其中编写、打开、保存和运行 Python 脚本。
    属性:
        output_window (QMainWindow): 用于显示代码执行结果的窗口。
        text_edit (QTextEdit): 用于输入 Python 代码的文本编辑器。
    """

    def __init__(self):
        """
        初始化 MainWindow 类的实例。
        参数:
            无
        返回:
            无
        """

    def initUI(self):
        """
        初始化用户界面。
        设置文本编辑器、状态栏、菜单栏和其他 GUI 组件。
        参数:
            self: 调用此函数的 MainWindow 对象。
        返回:
            无
        """

    def newFile(self):
        """
        清除文本编辑器的内容以创建新文件。
        此函数将文本编辑器的内容设置为空字符串，允许用户从头开始编写新的 Python 代码。
        参数:
            self: 调用此函数的 MainWindow 对象。
        返回:
            无。但文本编辑器将被清空。
        """

    def showDialog(self):
        """
        打开文件对话框并读取文件内容。
        此函数允许用户从主目录选择一个文件，然后读取文件内容并在 QTextEdit 小部件中显示。
        参数:
            self: 调用此函数的 MainWindow 对象。
        返回:
            无，但函数会在窗口中显示代码。
        异常:
            FileNotFoundError: 如果文件不存在或无法打开。
            IOError: 如果读取文件时出现错误。
        """

    def saveDialog(self):
        """
        打开文件对话框并保存文本编辑器的内容。
        此函数允许用户选择文件名和位置，然后将文本编辑器的内容写入文件。
        参数:
            self: 调用此函数的 MainWindow 对象。
        返回:
            无。但此函数将保存 Python 文件。
        异常:
            FileNotFoundError: 如果文件无法创建或打开。
            IOError: 如果写入文件时出现错误。
        """

    def runCode(self):
        """
        执行文本编辑器中的 Python 代码。
        此函数从文本编辑器中检索代码，将其写入名为 'code.py' 的文件，然后使用 subprocess 模块运行此文件。
        代码的输出被捕获并存储在 `result` 变量中。输出然后从字节解码为字符串。最后，删除 'code.py' 文件并打印输出。
        注意: 此函数可以动态运行 Python 代码，但它可以执行任何 Python 代码，如果代码来自不受信任的来源，这可能是危险的。
        始终确保验证和清理您收到的任何输入，如果您计划执行它。
        参数:
            self: 包含文本编辑器和要执行的代码的对象的实例。
        返回:
            无。但此函数将在执行 Python 代码后打印输出和错误。
        异常:
            可能引发由执行的 Python 代码或文件操作引发的任何异常。
        """

    def findFunctionsAndClasses(self):
        """
        显示列出代码编辑器中所有函数和类的窗口。
        此方法从文本编辑器中的当前代码中提取所有函数和类，并在新的 FunctionAndClassWindow 中显示它们。
        在此窗口中点击函数或类名将导航到主代码编辑器中的位置。
        返回:
            无。将显示带有函数和类列表的新 FunctionAndClassWindow。
        """
```

这些 docstrings 为 `MainWindow` 类及其方法提供了中文描述，帮助理解类的功能和每个方法的作用。